


  <p>Hello, you are an Admin</p>
  <h2>Users</h2>
  <% @users = User.all %>
  <% @users.each do |user| %>
    <p><%= link_to "Delete User", user, :confirm => "Are you sure?", :method => :delete %>
  <% end %>
<% else %>
  <p>Hello, you are not an Admin</p>
